#include <stdio.h>
#include <string.h>

char* longestPalindrome(char* s) {
    
    int len = strlen(s);
    int dp[len][len];
    
    return s;

}

int main(int argc,char* argv[]){

	printf("Result:%s\n",longestPalindrome(argv[1]));
}